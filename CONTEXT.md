# CONTEXT — 用語集（ギター指板トレーナー）

このアプリの「練習エンジン化」にあたって、用語の意味を一意に固定する。
実装詳細は書かない（用語の定義のみ）。決定の経緯は `docs/adr/` を参照。

## 練習エンジン (Practice Engine)
このアプリの北極星。1問ごとの回答を記録し、弱点を可視化し、次の出題を適応的に最適化する中核機構。
「指板を見て考えるアプリ」ではなく「指板を反射で使えるようにする」ためのもの。
横展開（新スケール・新コードの表示追加）より、この機構の構築を優先する。

## 反射化 (Reflexive Recall)
目標とする到達状態。「どの音がどこにあるか」を考えずに把握できること。
正誤だけでなく**回答時間**で測る。知っていても3秒かかるなら演奏では使えない、という立場。

## セッション (Session)
区切りのある練習の1単位。**すべての練習はセッション**であり、終了時に必ず結果画面が出る。
- **デイリー練習 (Daily Practice)**: 固定長（目安 2分 / 約35問）のセッション。前日までの弱点を優先出題する。
- **自由練習 (Free Practice)**: 範囲をカスタムし、無制限に続けられるセッション。ユーザーが「終了」を押して締める。

## 結果画面 (Result Screen)
セッション終了時に必ず表示する画面。正答率・回答時間（平均/中央値）・**前回比**・ヒートマップを示す。
「練習した感」ではなく「昨日より速く・正確になった実感」を与えることが目的。

## 出題対象 (Target)
弱点を記録・集計する単位。クイズ種別ごとに意味が異なる:
- **音名認識 (position↔note)**: target = (弦, フレット)。指板ヒートマップで色付けする単位はこれ。
- **度数 (interval)**: ルート依存のため (弦, フレット) では混ざる。度数そのもの（b3 など）を単位とする別指標で扱い、指板ヒートマップには混ぜない。
ログ自体は全クイズで取得するが、「指板ヒートマップ」は音名認識のみを可視化する（混ぜると弱点の意味が濁るため）。

## 弱点スコア (Weakness Score)
適応出題を駆動する、出題対象（Target）ごとのスコア。誤答率・反応速度・最終練習からの経過・カリキュラム重要度から算出する（重み付けは別途決定）。

## フェーズ (Phase)
到達目標を段階で示す「到達マップ」。クリアでバッジが付くが**ロックはしない**（いつでも好きなフェーズに飛べる）。
フェーズは大枠の地図にすぎず、フェーズ内で「次にどの問題を出すか」は弱点スコアが決める。
人が決めた固定の階段（フェーズ）と適応エンジン（弱点スコア）の役割分担: 地図=フェーズ、現在地での一手=エンジン。

## ヒートマップ (Heatmap)
指板上に弱点を色で重ねる表示。色の濃さ=反応速度、赤系=誤答率、無色=未出題。

## リファレンス表示 (Reference Views)
既存の「見るだけ」のビュー群（指板マップ / スケール / CAGED / ボイシング / オープン / ダイアトニック / アルペジオ）。
練習エンジンを主役とした上での**脇役**と位置づける。
